# 楞严经产品基线

**状态：** 当前产品约束摘要
**更新：** 2026-07-26

本文件只描述《楞严经》当前产品边界和必须保持的用户结果，不重复代码、schema 或实施计划。工程与验证规则以根目录 `AGENTS.md` 和 `./verify.sh` 为准；多产品方向以 `.planning/` 中三份主计划为准。

## 1. 产品定位

《楞严经》是一款安静、离线优先、无需账户和广告的单经阅读与听诵 App。核心用户包括每日读诵的中老年修行者、通勤听经的用户，以及需要搜索和引用原文的研究者。

最重要的用户结果是：

1. 打开后能够可靠接续上次阅读或同一卷听诵位置。
2. 正文清晰、可放大，在手机和 iPad 上不被导航栏、Tab Bar 或安全区遮挡。
3. 搜索、目录、收藏、分享和复制都能回到准确的经文位置。
4. 音频准备过程有状态和错误反馈，切换卷时不提前中断正在播放的内容。
5. 主题、简繁体和 Widget 在重启、快速切换和系统尺寸变化后保持可读。

## 2. 当前生产功能

- 科判目录、分页阅读、精读阅读和卷阅读。
- 阅读位置、目录展开状态和收藏持久化。
- 全文搜索及搜索结果定位。
- 纯文本复制、文字文件分享、单图或自动分页图片分享。
- 后台音频、锁屏控制、同卷续播、播放模式、下载进度和错误反馈。
- iOS 15-25 Legacy ODR、iOS 26+ Managed Background Assets，以及校验后才可播放的 CDN 应急回退。
- 简繁显示、可读字号、阅读主题、每日提醒和反馈入口。
- 小/中/大桌面 Widget 与系统支持的锁屏 Widget。
- iPhone、iPad split view、横竖屏和安全区适配。

以上能力不是静态清单；实际行为由当前测试、代码和已接受的契约定义。修改前应先查找对应回归，不得根据旧报告重新实现已经存在的功能。

## 3. 不变约束

- iOS 主 App 最低系统保持 iOS 15；iOS 26 下载扩展必须通过弱链接和可用性边界，不能提高主 App 最低版本。
- 正文必须离线可读。网络只用于按需音频、用户主动反馈和明确记录的发布服务。
- 不增加登录、广告、社交信息流、强制云同步或 AI 解经。
- 不因视觉留白牺牲正文可读性、可访问字号、按钮触达范围或紧凑设备内容量。
- 不把设备页码作为跨版本内容身份；进度、收藏和深链使用稳定内容 ID，并为旧路径提供迁移。
- 不允许 AI 改写、补写或静默规范化经文。任何新底本必须有来源、版本、checksum、权利依据和人工审定记录。

## 4. 多产品边界

后续《金刚经》《圆觉经》《六祖坛经》是独立 App，不是《楞严经》内的书架。它们共享内容 schema、稳定 ID、验证器和平台内公共模块，但拥有独立应用身份、Widget、用户数据和发布周期。

第二产品建立前必须先完成共享内容契约和来源治理。推荐顺序是：

```text
楞严经基线与共享契约
  -> 金刚经
  -> 圆觉经
  -> 六祖坛经
```

Android 先以《楞严经》建立生产级原生基准，再用《金刚经》验证多产品复用；不同时复制四个未完成工程。

## 5. 发布门槛

每次发布至少需要：

- `./verify.sh all` 通过，相关 UI smoke 通过。
- 经文、来源或生成规则变更通过 schema、checksum、简繁差异和稳定 ID 迁移检查。
- 音频变更通过 catalog 生成校验、字节数、SHA-256、Archive 结构和真机播放检查。
- iPhone/iPad 关键阅读、收藏、主题、分享和 Widget 流程没有遮挡、白屏或状态丢失。
- 隐私说明与实际反馈、Apple 音频和 CDN 数据流一致。
- 人工批准经文准确性、权利、永久应用身份、签名和正式商店提交。

当前仍需在真实发行环境关闭的事项记录在 `docs/quality/REFERENCE_BASELINE_2026-07-26.md` 和 `BackgroundAssets/IMPLEMENTATION_STATUS_2026-07-19.md`。

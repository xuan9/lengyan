# Agent Entry Point

Read and follow [`AGENTS.md`](AGENTS.md). It is the authoritative repository
guide for architecture, verification, scripture integrity, source governance,
and change discipline.

The public clean-clone command is:

```bash
./verify.sh all
```

Do not restore historical instructions from this file. Current product and
platform plans live under `.planning/`, while executable contracts and tests
take precedence over plans.
